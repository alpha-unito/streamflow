version: v1.0
workflows:
  conformance-test:
    config:
      docker:
        - step: /
          deployment:
            type: singularity
            config: {}
database:
  type: default
  config:
    connection: ":memory:"