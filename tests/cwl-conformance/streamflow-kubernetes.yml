version: v1.0
workflows:
  conformance-test:
    config:
      docker:
        - step: /
          deployment:
            type: kubernetes
            config:
              networkPolicy: true
database:
  type: default
  config:
    connection: ":memory:"